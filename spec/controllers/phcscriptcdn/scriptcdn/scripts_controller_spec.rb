require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

module Phcscriptcdn
  RSpec.describe Scriptcdn::ScriptsController, type: :controller do

    # This should return the minimal set of attributes required to create a valid
    # Scriptcdn::Script. As you add validations to Scriptcdn::Script, be sure to
    # adjust the attributes here as well.
    let(:valid_attributes) {
      skip("Add a hash of attributes valid for your model")
    }

    let(:invalid_attributes) {
      skip("Add a hash of attributes invalid for your model")
    }

    # This should return the minimal set of values that should be in the session
    # in order to pass any filters (e.g. authentication) defined in
    # Scriptcdn::ScriptsController. Be sure to keep this updated too.
    let(:valid_session) { {} }

    describe "GET #index" do
      it "assigns all scriptcdn_scripts as @scriptcdn_scripts" do
        script = Scriptcdn::Script.create! valid_attributes
        get :index, {}, valid_session
        expect(assigns(:scriptcdn_scripts)).to eq([script])
      end
    end

    describe "GET #show" do
      it "assigns the requested scriptcdn_script as @scriptcdn_script" do
        script = Scriptcdn::Script.create! valid_attributes
        get :show, {:id => script.to_param}, valid_session
        expect(assigns(:scriptcdn_script)).to eq(script)
      end
    end

    describe "GET #new" do
      it "assigns a new scriptcdn_script as @scriptcdn_script" do
        get :new, {}, valid_session
        expect(assigns(:scriptcdn_script)).to be_a_new(Scriptcdn::Script)
      end
    end

    describe "GET #edit" do
      it "assigns the requested scriptcdn_script as @scriptcdn_script" do
        script = Scriptcdn::Script.create! valid_attributes
        get :edit, {:id => script.to_param}, valid_session
        expect(assigns(:scriptcdn_script)).to eq(script)
      end
    end

    describe "POST #create" do
      context "with valid params" do
        it "creates a new Scriptcdn::Script" do
          expect {
            post :create, {:scriptcdn_script => valid_attributes}, valid_session
          }.to change(Scriptcdn::Script, :count).by(1)
        end

        it "assigns a newly created scriptcdn_script as @scriptcdn_script" do
          post :create, {:scriptcdn_script => valid_attributes}, valid_session
          expect(assigns(:scriptcdn_script)).to be_a(Scriptcdn::Script)
          expect(assigns(:scriptcdn_script)).to be_persisted
        end

        it "redirects to the created scriptcdn_script" do
          post :create, {:scriptcdn_script => valid_attributes}, valid_session
          expect(response).to redirect_to(Scriptcdn::Script.last)
        end
      end

      context "with invalid params" do
        it "assigns a newly created but unsaved scriptcdn_script as @scriptcdn_script" do
          post :create, {:scriptcdn_script => invalid_attributes}, valid_session
          expect(assigns(:scriptcdn_script)).to be_a_new(Scriptcdn::Script)
        end

        it "re-renders the 'new' template" do
          post :create, {:scriptcdn_script => invalid_attributes}, valid_session
          expect(response).to render_template("new")
        end
      end
    end

    describe "PUT #update" do
      context "with valid params" do
        let(:new_attributes) {
          skip("Add a hash of attributes valid for your model")
        }

        it "updates the requested scriptcdn_script" do
          script = Scriptcdn::Script.create! valid_attributes
          put :update, {:id => script.to_param, :scriptcdn_script => new_attributes}, valid_session
          script.reload
          skip("Add assertions for updated state")
        end

        it "assigns the requested scriptcdn_script as @scriptcdn_script" do
          script = Scriptcdn::Script.create! valid_attributes
          put :update, {:id => script.to_param, :scriptcdn_script => valid_attributes}, valid_session
          expect(assigns(:scriptcdn_script)).to eq(script)
        end

        it "redirects to the scriptcdn_script" do
          script = Scriptcdn::Script.create! valid_attributes
          put :update, {:id => script.to_param, :scriptcdn_script => valid_attributes}, valid_session
          expect(response).to redirect_to(script)
        end
      end

      context "with invalid params" do
        it "assigns the scriptcdn_script as @scriptcdn_script" do
          script = Scriptcdn::Script.create! valid_attributes
          put :update, {:id => script.to_param, :scriptcdn_script => invalid_attributes}, valid_session
          expect(assigns(:scriptcdn_script)).to eq(script)
        end

        it "re-renders the 'edit' template" do
          script = Scriptcdn::Script.create! valid_attributes
          put :update, {:id => script.to_param, :scriptcdn_script => invalid_attributes}, valid_session
          expect(response).to render_template("edit")
        end
      end
    end

    describe "DELETE #destroy" do
      it "destroys the requested scriptcdn_script" do
        script = Scriptcdn::Script.create! valid_attributes
        expect {
          delete :destroy, {:id => script.to_param}, valid_session
        }.to change(Scriptcdn::Script, :count).by(-1)
      end

      it "redirects to the scriptcdn_scripts list" do
        script = Scriptcdn::Script.create! valid_attributes
        delete :destroy, {:id => script.to_param}, valid_session
        expect(response).to redirect_to(scriptcdn_scripts_url)
      end
    end

  end
end

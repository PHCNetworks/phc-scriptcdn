<%= form_for(script_author) do |f| %>
  <% if script_author.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(script_author.errors.count, "error") %> prohibited this script_author from being saved:</h2>

      <ul>
      <% script_author.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
